import 'package:flutter/material.dart';

// A function that returns a TextStyle for card titles.
TextStyle cardTitleStyle() {
  return const TextStyle(
    fontFamily: 'Proxima-Nova', // Sets the font family to 'Proxima-Nova'.
    fontSize: 21, // Sets the font size to 21.
  );
}
